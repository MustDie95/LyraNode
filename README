1) Write current Lyra version to .env file

2) Start containers with: docker-compose -f lyra.yaml up -d --build

3) Look into logs:
  
  docker logs --follow --timestamps --details --tail 10 mongodb

  docker logs --follow --timestamps --details --tail 10 lyra.node
